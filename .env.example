# Secretos de OpenClaw. Copia este archivo a .env y rellena los valores.
# Formato apto para docker-compose env_file: el valor es TODO lo que sigue al '=',
# por eso los comentarios van en su propia linea (no en la misma del valor).
# NO commitees el .env real (esta en .gitignore).

# --- Claves de proveedores (un agente por proveedor -> cuotas independientes) ---
# NVIDIA build (orquestador, PRIMARIO - GRATIS): https://build.nvidia.com  (la key empieza por nvapi-)
NVIDIA_API_KEY=
# Google AI Studio: YA NO SE USA. Google empezo a cobrar (limite de 1 EUR alcanzado) y se ha sacado
# del config por completo (orquestador -> NVIDIA; memoria RAG -> OpenRouter). Puedes dejar esto vacio.
GEMINI_API_KEY=
# Groq (STT: transcripcion de notas de voz de Telegram con Whisper, GRATIS): https://console.groq.com
# Necesaria para que el orquestador ENTIENDA AUDIOS (audio -> texto -> modelo). Sin ella, los
# audios no se transcriben (el texto y las imagenes funcionan igual). El alta de Groq fallaba antes;
# reintentala (es gratis, sin tarjeta). Alternativas STT gratis: Mistral (Voxtral) o Deepgram.
GROQ_API_KEY=
# Cerebras (tecnico + formato): https://cloud.cerebras.ai
CEREBRAS_API_KEY=
# OpenRouter: ahora OBLIGATORIA. Doble uso: (1) ultimo fallback de chat (modelo :free) y
# (2) embeddings de la memoria RAG (nvidia/llama-nemotron-embed-vl-1b-v2:free). https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Gateway ---
# Token para autorizar dispositivos. Genera uno con:  openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# --- Servicios externos via MCP (Klavis Strata): Calendar + Notion + Drive en UN solo MCP ---
# 1) API key en klavis.ai.  2) Crea UNA strata con un POST (ver README) -> te devuelve la
#    strataServerUrl (UNA sola, estable por userId) y los oauthUrls para autorizar las cuentas.
# 3) Pega aqui la API key y la strataServerUrl (la auth va por cabecera Bearer con la key).
KLAVIS_API_KEY=
KLAVIS_STRATA_URL=

# --- Telegram (chat desde el movil) ---
# Token del bot creado con @BotFather en Telegram.
TELEGRAM_BOT_TOKEN=
# Tu ID numerico de Telegram (escribe a @userinfobot para obtenerlo). Solo este ID podra
# hablar con el orquestador (dmPolicy allowlist).
TELEGRAM_ALLOW_FROM=

# --- Busqueda web ---
# Proveedor: DuckDuckGo (forzado en openclaw.json). GRATIS, sin clave, sin contenedor extra
# (elegido por RAM: SearXNG no cabe junto a OpenClaw en la e2-micro de 1GB). Nada que poner aqui.

# --- Opcionales ---
# Interfaz del host donde publicar el puerto 18789. Por defecto 127.0.0.1 (solo local/tunel).
# Para acceso directo por IP publica (con firewall restringido a tu IP): OPENCLAW_BIND=0.0.0.0
# OPENCLAW_BIND=0.0.0.0
# Salta la verificacion de modelos al arrancar:
# SKIP_MODEL_PREFLIGHT=1
# Token del tunnel de Cloudflare (solo si usas el perfil "tunnel" del docker-compose):
# CLOUDFLARE_TUNNEL_TOKEN=
